{
  "name": "sub2api-rate-checker",
  "version": "0.0.9",
  "description": "Desktop rate comparison console for sub2api and New API relay stations.",
  "main": "src/main.js",
  "private": false,
  "license": "MIT",
  "keywords": [
    "sub2api",
    "new-api",
    "electron",
    "rate-checker",
    "relay"
  ],
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/storage.js && node --check src/sub2apiClient.js && node --check src/renderer/renderer.js && node --check scripts/check-rate-format.js && node --check scripts/check-newapi-client.js && node --check scripts/check-comparison-groups.js && node --check scripts/check-site-overview.js && node --check scripts/check-storage-cache.js && node --check scripts/check-speed-test.js && node --check scripts/smoke-ui.js && node scripts/check-rate-format.js && node scripts/check-newapi-client.js && node scripts/check-comparison-groups.js && node scripts/check-site-overview.js && node scripts/check-storage-cache.js && node scripts/check-speed-test.js",
    "smoke:ui": "electron scripts/smoke-ui.js",
    "test": "npm run check",
    "dist": "electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "local.sub2api.ratechecker",
    "productName": "Sub2API Rate Checker",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "portable"
      ]
    }
  }
}
